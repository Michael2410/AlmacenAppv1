# ========================================
# AlmacenApp - Configuración de Producción
# ========================================

# Puerto del servidor (default: 3001)
PORT=3001

# Secret para JWT - CAMBIAR EN PRODUCCIÓN
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTE_SECRETO_EN_PRODUCCION_POR_UNO_ALEATORIO_Y_SEGURO

# Entorno de ejecución
NODE_ENV=production

# Configuración de base de datos (opcional)
# DB_PATH=./almacen.db

# Configuración de CORS (opcional)
# CORS_ORIGIN=https://tu-dominio.com

# Configuración de logging (opcional)
# LOG_LEVEL=info
